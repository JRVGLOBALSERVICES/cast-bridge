{
  "name": "cast-bridge",
  "private": true,
  "version": "1.0.0",
  "description": "Paste a link, play it, cast it to the TV.",
  "dependencies": {
    "@sparticuz/chromium": "^131.0.0",
    "puppeteer-core": "^23.11.1"
  },
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "test": "node scripts/test-unpack.js && node scripts/test-reissue.js",
    "test:unpack": "node scripts/test-unpack.js",
    "test:reissue": "node scripts/test-reissue.js"
  }
}
